import 'package:flutter/material.dart';

class CRUDSnackBar extends StatelessWidget {
  final String title;
  CRUDSnackBar({Key? key, required this.title}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
